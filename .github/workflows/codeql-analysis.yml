name: "CodeQL Analysis: cFS-Bundle"

on:
  push:
  pull_request:

jobs:
  codeql:
    uses: nasa/ArielSAdams/.github/workflows/codeql-reusable.yml@test-paths-ignore
    with: 
      component-path: cFS
      make: make -j8
      test: true
