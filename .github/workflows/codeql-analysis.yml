name: "CodeQL Analysis: cFS-Bundle"

on:
  push:
    paths-ignore: 
    - '**/*.md'
    - '**/*.txt'
    - '**/*.dox'

  pull_request:
    paths-ignore: 
    - '**/*.md'
    - '**/*.txt'
    - '**/*.dox'

jobs:
  codeql:
    uses: ArielSAdams/cFS/.github/workflows/codeql-reusable.yml@New-CodeQL-Config
    with: 
      component-path: cFS
      make: make -j8
      test: true
